import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Conversación compartida | Chat Sara | INXORA',
  description: 'Ver conversación compartida con Sara Xora, asistente virtual de INXORA.',
}

export default function ChatSaraShareLayout({ children }: { children: React.ReactNode }) {
  return children
}
